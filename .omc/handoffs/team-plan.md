## Handoff: team-plan → team-exec

- **Decided**: Source of truth is `.omc/plans/lh-winner-auth-page.md` (Architect+Critic approved, 4-round consensus). Architecture: OCR preview stays advisory-only on the existing 192.168.1.3 home server (reached via Cloudflare Tunnel, same pattern as `chat.html`'s `API_URL`); the write path (Drive upload + Sheets append) is a Google Apps Script Web App the client calls directly with the image inlined as base64 — no dependency on 192.168.1.3 for successful submission.
- **Rejected**: v1 (append columns directly to the existing Google Form's response tab — rejected, corrupts Form-owned columns); v2 (Apps Script pulls the image from 192.168.1.3 at submit time — rejected, silently reintroduces the tunnel dependency the redesign was meant to remove).
- **Risks**: This repo (`e-park-hansang-bundang`) only contains the static frontend. The two backend pieces (FastAPI addition on 192.168.1.3, and the Google Apps Script project) are NOT deployable from this working directory — no SSH access to the home server, no Google OAuth session for `clasp`/Apps Script deploy. Workers must produce ready-to-deploy source files + a deployment README instead of attempting live deployment.
- **Files**: `.omc/plans/lh-winner-auth-page.md` (full implementation spec incl. a complete, bug-fixed Apps Script `doPost` code sample), `.omc/specs/deep-interview-lh-winner-auth-page.md` (original requirements), `qna.html` (template to clone for `auth-page.html`'s look/structure), `chat.html` (existing OCR-adjacent fetch/API_URL pattern to mirror).
- **Remaining**: (1) Build `auth-page.html` per plan Implementation Step 1. (2) Materialize the plan's Apps Script code sample into `backend/apps-script/Code.gs` plus a deployment README, and write the 192.168.1.3 FastAPI OCR endpoint addition as `backend/rag-server-ocr-endpoint.py` plus deployment notes for README's "서버 관리" section. (3) Verify against the plan's Acceptance Criteria list (as many as are testable without live backend access) and confirm the deliverable code matches the plan's approved code sample byte-for-byte where the plan gives exact code.
